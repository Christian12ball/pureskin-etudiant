# Nixpacks configuration for Railway Frontend deployment

[phases.setup]
nixPkgs = ["...", "nodejs-20_x"]

[phases.build]
cmds = ["npm install", "npm run build"]

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production"
PORT = "3000"
